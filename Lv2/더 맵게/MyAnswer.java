import java.util.Arrays;

class Solution {
    public int solution(int[] scoville, int K) {
        int answer = 0;
        return answer;
    }
    
    public int mix(int index,int goal, int[] a) {
    	
    	if(goal)
    		return 1;
    	else {
    		Arrays.sort(a);
    		return mix(index,)
    	}
    		
    	
    }
}